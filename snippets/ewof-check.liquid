{%- comment -%}
  EyeWipes offer page — blue check bullet icon.
  Accepts: size (number, px)
{%- endcomment -%}
{%- assign s = size | default: 19 -%}
<svg width="{{ s }}" height="{{ s }}" viewBox="0 0 24 24" fill="none" aria-hidden="true"><circle cx="12" cy="12" r="9.5" fill="#47B5E9"></circle><path d="m7.5 12.2 3 3 6-6.4" stroke="#FFFFFF" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" fill="none"></path></svg>
